<!DOCTYPE html>
<html>
  <head>
    <title>Komod</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/bars/navbar" %>
    <%= render "shared/flashes" %>

<%# C'est juste un test à enlever %>
<%# au dessus, les images marchent, vous pouvez supprimer %>

    <div class="main-content">
      <%= yield %>
    </div>

    <%= render "shared/bars/tapbar" %>
  </body>
</html>
