<h1>Mon profil</h1>
<p>Photo de profil</p>
<h3><%= @user.user_name %></h3>
<p><%= @user.first_name%> <%= @user.last_name %></p>
<%= link_to "Modifier mon profil", edit_profils_path(@user) %>

<h2>Tableau de bord</h2>

<p> Mes emprunts actuel </p>
<% unless @transctions.nil? %>
  <% @transactions.each do |transaction| %>
    <%= transaction.clothe.name %>
    <p>render card emprunts ici</p>
  <% end %>
<% end %>
<p> Mes prêts actuel </p>
<% @clothes.each do |clothe| %>
  <%= clothe.name %>
  <% unless clothe.transactions.empty? %>
    <p><%=clothe.name%> est loué </p>
  <% end %>
  <p>render card pret</p>
<% end %>

<%= link_to "mes transactions", owner_transactions_path %>

<p> Les évènements où je suis présent </p>
